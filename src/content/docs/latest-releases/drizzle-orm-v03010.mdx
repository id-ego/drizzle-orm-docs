---
title: DrizzleORM v0.30.10 릴리스
pubDate: 2024-05-01
description: 모든 WHERE 표현식에 '.if()' 함수가 추가되었으며, AWS DataAPI의 세션 '.all', '.values', '.execute' 함수에 대한 내부 매핑이 수정되었습니다.
---

## 새로운 기능

### 🎉 모든 WHERE 표현식에 `.if()` 함수 추가

#### 조회수가 100보다 큰 모든 게시물 선택

```ts {5}
async function someFunction(views = 0) {
  await db
    .select()
    .from(posts)
    .where(gt(posts.views, views).if(views > 100));
}
```

## 버그 수정

- AWS DataAPI의 세션 `.all`, `.values`, `.execute` 함수에 대한 내부 매핑 수정

AWS DataAPI 시작 가이드는 [문서](/docs/get-started-postgresql#aws-data-api)를 참고하세요.
