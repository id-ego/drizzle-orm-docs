<!-- TODO: Algolia DocSearch 신규 신청 후 appId, apiKey, indexName 업데이트 필요 -->
<!-- 신청: https://docsearch.algolia.com/apply -->
<div class="nav_search-wrap">
  <div class="search-wrap--centered" id="docsearch"></div>
</div>
<script type="module" is:inline data-astro-rerun>
  docsearch({
    container: '#docsearch',
    // TODO: DocSearch 승인 후 아래 값들을 새로운 값으로 교체
    appId: "MXZNUT59HV",           // 새 appId로 교체
    apiKey: "7c390e262ac8858df0b6624d01d9dfef",  // 새 apiKey로 교체
    indexName: "orm-drizzle",      // 새 indexName으로 교체
    placeholder: "문서 검색...",
  });
</script>
<style>
  .nav_search-wrap {
    align-items: center;
    display: flex;
    flex-grow: 1;
    gap: 12px;
    max-width: 420px;
  }

  @media screen and (max-width: 768px) {
    .nav_search-wrap {
      display: none;
    }
  }
</style>
